//finding ascii values
//ascii- american standard code for information interchange

let character = prompt("Enter a character: ");

let result = character.charCodeAt();

// to get at a point we can use charPointAt() method

console.log(`ascii value of ${character} is ${result}`);